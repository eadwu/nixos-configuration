(defvar user-emacs-cache-directory (concat user-emacs-directory "cache/")
  "Volatile storage directory.")

(defvar user-emacs-core-directory (concat user-emacs-directory "core/")
  "Core 'hacking' directory.")

(defvar user-emacs-modules-directory (concat user-emacs-directory "modules/")
  "'Hacking' modules directory.")

(provide 'constants)
